'use strict';

function createCharge() {
  // const chargeObj = {
  //
  //
  // }
}

function build(db, cb) {
  cb(null, 'success');
}

module.exports = {
  build: build,
  createCharge: createCharge
};
